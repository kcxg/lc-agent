# CONTEXT.md — Token 统计领域术语表

> 唯一职责：定义 token 用量统计领域的词汇。实现细节一律在 `docs/tasks/token_stats.md`。

## 模型标识

- **model_id** — 配置者自己命名的、全局唯一的模型名。身份、统计、显示的唯一标识。原厂直连下必须等于厂商原始模型名（发给 API 的就是它）。
- **raw_model_id** — 渠道提供的原始模型名。同一个底层模型在不同渠道会有不同的 model_id，但 raw_model_id 相同。允许与 model_id 相等（原厂直连时这是唯一正确答案）。
- **provider** — `base_url` + `api_key` 的配置分组，仅此而已。不参与标识、定价、统计的任何键。
- **马甲 / 路由** — 同一个底层模型在某渠道下的不同入口名（如 `zzz-auto-gpt-5.4`）。按唯一性规则，一个渠道一个底层模型只许一个入口；多路由是渠道侧（litellm）的事。

## 用量与账单

- **用量行** — 一次 LLM 调用的一行原始消耗记录（不是"一轮对话"，一轮可能含多次调用）。只存 token 数，永不存金额。
- **主会话 / 子 agent** — 一条用量行产生于主会话的 LLM 调用，还是子 agent（嵌套会话）的调用。子 agent 由主会话发起，消耗归属见下。
- **归属** — 子 agent 的用量行同时记录：实际执行的子 agent、发起会话的用户、主会话。看板用"含子 agent / 仅主 agent"开关切换口径，默认含子 agent。
- **调用来源** — 用量行的产生场景：对话、标题生成、上下文压缩、自动化任务。
- **run / seq** — 一次会话请求（invoke）内的一组调用，以序号区分其中的第几次 LLM 调用。二者合起来保证中断恢复重放不重复计数。

## 金额

- **单价** — 某模型某 token 种类（输入 / 输出 / 缓存读 / 缓存写）每百万 token 的价格，管理员在页面维护。
- **生效时间** — 单价的起始生效日期，精确到天。改价 = 新增一条带新生效时间的单价，绝不覆盖旧价。
- **按当时价计算** — 金额永远在查询时用"统计时间点当时生效"的单价计算，历史账单不因日后调价而变化。
- **未配价** — 某模型没有任何可用单价。金额显示 `—`（不是 0），token 数照常显示。
- **记账币种** — 全系统唯一币种（CNY）。渠道按外币计费时由配置者折算录入，系统内不换算。

## 统计视角

- **切桶** — 把用量行按时间归组（天 / 月）。桶边界按服务器本地时区计算，`ts` 本身存 UTC。
- **归并视角** — 按 raw_model_id 分组，把同一底层模型在各渠道的用量合并成一行看总量。与按 model_id 分组的金额合计必须一致——归并只换视角，不改数。默认分组是 model_id，归并是用户主动切换的视角。
