# lc_agent/tools/builtin_tools/__init__.py
# Tools registered with group="__builtin__" are always injected into every agent,
# regardless of allowed_tool_groups. The __builtin__ group mechanism is preserved
# here for future built-in tools; ask_user has moved to lc_agent.middlewares.
